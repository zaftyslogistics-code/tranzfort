-- Rollback stub for get_supplier_trips (P3 pattern — apply only if list RPC must be reverted)
-- Rename to .sql and run manually; not applied by default migrations.

DROP FUNCTION IF EXISTS get_supplier_trips(UUID, TEXT[], INT, INT);
